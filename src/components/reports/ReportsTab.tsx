/**
 * Removed from Manager Dashboard (export is under Export tab).
 * Stub file kept so Vite HMR does not reference a missing module after hot reload.
 */
export function ReportsTab(_props: { projectId: string }) {
  return null;
}
